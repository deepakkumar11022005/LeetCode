# Write your MySQL query statement below
select p.product_name ,sum(o.unit) unit from  orders o join products p on o.product_id =p.product_id and month(o.order_date)=2  and year(o.order_date)=2020 group by month(o.order_date),p.product_id having unit >=100;